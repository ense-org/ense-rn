// @flow

export type EnseJSON = {
  +url: string,
  +subtitle: string,
  +metadata: ?Object,
  +title: string,
  +altList: ?string,
  +order: number,
};

export type FeedJSON = {
  +url: string,
  +subtitle: string,
  +metadata: ?Object,
  +title: string,
  +altList: ?string,
  +order: number,
};
